import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Largify | One Company. Two Engines. One Ecosystem.",
  description:
    "Largify Solutions (Execution Engine) and Largify Innovation Lab (Capability Engine). A unified ecosystem for enterprise digital transformation and talent development.",
  keywords: [
    "digital transformation",
    "enterprise solutions",
    "innovation lab",
    "capability building",
    "cybersecurity",
    "ERO system",
    "Pakistan technology",
  ],
  icons: {
    icon: "/favicon.ico",
    shortcut: "/favicon.ico",
    apple: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-slate-950 text-slate-50`}
      >
        {children}
      </body>
    </html>
  );
}
